import React from 'react';
import ReactDOM from 'react-dom/client';

// Its for TodoList
import App from './AllReactApps/TodoList/App';

// Its for NetflixClone
// import App from './AllReactApps/NetflixClone/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
